box: python:2.7

services:
   - id: postgres
     env:
       POSTGRES_PASSWORD: cb

build:
  steps:

    - virtualenv:
        name: setup virtual environment
        install_wheel: true

    - pip-install

    - script:
        name: django test
        code: |
          python manage.py test apps.api2 -v3
